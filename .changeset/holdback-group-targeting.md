---
"@guardian/support-dotcom-components": minor
---

Add holdback group targeting support across all RRCP components (banners, epics, headers, gutters). Users with `inHoldbackGroup === true` are now only eligible for tests that contain "HOLDBACK" in their name, enabling proper control group experimentation without requiring changes to the RRCP tool.
